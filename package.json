{
  "name": "terraform-docker",
  "version": "1.0.2",
  "description": "Docker with terraform and bash",
  "repository": "git@github.com:souravworkspace/terraform-docker.git",
  "author": "Sourav Halder",
  "scripts": {
    "dev": "docker build .",
    "postversion": "git push origin && git push origin --tags"
  }
}
